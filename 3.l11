# PROGRAM pg;
START VARIABLE INTEGER32 aa , bb ,  ca , cb , xx;

  GET (aa);
  GET (bb);
  
  aa->cb;   
  
la:
  IF cb >= bb + 1 
  {
     GOTO lb;
  }

  PUT(cb*cb);
  cb+1->cb;
  GOTO la;
  
lb:
  1->ca;
  0->xx;

ls:
  IF ca >= aa + 1 
  {
     GOTO lk;
  }
    
  1->cb;  
  
lc:
  IF cb >= bb + 1
  {
     GOTO ld;
  }
  
  xx+1->xx;
  cb+1->cb;
  GOTO lc;
  
ld:
  ca+1->ca;
  GOTO ls;
lk:
   PUT (xx);

STOP
